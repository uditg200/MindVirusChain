services:
  - type: web
    name: virus-chain-viewer
    runtime: python
    buildCommand: pip install .
    startCommand: viewer --port $PORT --host 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: PORT
        value: "10000"
