[project]
name = "figures"
version = "0.1.0"
description = "Figure regeneration scripts for virus_chain experiments"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10",
    "numpy>=2.0",
    "pandas>=2.0",
    "pyarrow>=18.0",
]

[tool.hatch.build.targets.wheel]
packages = []
